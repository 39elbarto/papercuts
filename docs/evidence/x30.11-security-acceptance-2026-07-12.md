# x30.11 Security Acceptance Evidence

Date: 2026-07-12

Base commit: `af65e9053688e5d76460686d3194011b24126f33`

Scope: contract-2 single-project acceptance only

## Focused runner

Command:

```bash
PAPERCUTS_ACCEPTANCE_RUN_ID=x30.11-final scripts/security-acceptance.sh
```

Result: pass, 10 real-binary groups. The captured runner output replaced the
repo and shared Cargo target paths. A follow-up scan of the captured artifact
found no `/data/`, `/home/`, or fixture-sentinel values.

Covered groups:

- all high- and medium-risk categories under actual and dry-run refusal;
- stdout, stderr, missing target, missing parent, and existing-journal byte
  preservation;
- text, tags, explicit/environment agent, resolution note, and cross-field AWS
  pair scanning;
- exact two-part overrides and persisted override audit;
- false-positive controls and clean/warn/refuse/override dry-run behavior;
- private add/duplicate/dry-run/list/resolve/already-resolved/doctor/error path
  projection;
- committed HOME fallback, missing HOME, submodule identity, and documented
  committed-symlink compatibility;
- invalid UTF-8, parser/config redaction, mixed/malformed journals, and hostile
  irrelevant environment.

The complete test-to-contract mapping is in
`docs/SECURITY_ACCEPTANCE_MATRIX.md`.

## Full quality gate

Commands and results:

```text
bash -n scripts/security-acceptance.sh                                      pass
cargo build --release                                                       pass
cargo test --all-features                                                   pass: 17 unit + 53 CLI + 10 acceptance
cargo clippy --all-targets --all-features -- -D warnings                    pass
cargo fmt --check                                                           pass
git diff --check                                                            pass
gitleaks detect --source . --no-git --redact --exit-code 1                  pass: no leaks
```

The first Gitleaks run correctly flagged one synthetic assignment fixture. Its
source spelling was split with a Rust byte escape while retaining identical
runtime bytes; the final full working-tree scan found no leaks.

Scoped UBS was run directly against `tests/security_acceptance.rs`. Its
non-zero report consists of test-only `expect`/assert/panic surfaces, bounded
JSON indexing, and loop allocations in disposable fixtures. UBS independently
reported clean formatting, Clippy, Cargo check/test build, unsafe usage,
command construction, hardcoded-secret detection, async/concurrency hazards,
and resource lifecycle. No production code changed in this bead.

## Journal readback

The release binary was resolved through Cargo metadata because this host uses a
shared target directory. It ran:

```bash
papercuts --profile private --file .papercuts.jsonl doctor
```

Result before the final dogfood entry: exit 0, healthy, 19 lines, no findings.
The journal is historical and
therefore truthfully reports `legacy_path_records_retained:16` and
`legacy_unscanned_records:19`; this acceptance bead did not rewrite it.

The later staged-diff whitespace failure was appended as the clean contract-2
papercut `pc_ec1b21dae2ca`; it did not alter the prior 19 lines.

## Rollback

Revert the focused test, runner, matrix, and documentation commit. The runner
writes only ignored artifacts under `target/security-acceptance/`; removing
those artifacts is optional and does not affect journals or product behavior.
