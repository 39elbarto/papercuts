# x30.15 Combined Pilot Activation Evidence

Bead: `br-hardened-papercuts-fork-x30.15`

Activation time: `2026-07-12T16:07:01+07:00`

Earliest completion: `2026-07-26T16:07:01+07:00`

## Verdict

`ACTIVE` for the two aliases approved by `docs/PILOT_PLAN.md`. No other
repository is included. Wider adoption, multi-project design, and public
release remain blocked behind the completed 14-day evidence and `x30.16`
decision.

## Binary identity

- reviewed source SHA:
  `804d2b17e65edd865f3dc6e0ec05939aa65cf1ee`;
- installed binary SHA-256:
  `a74b2f926b72b7e4bc76039455e0e5cc8abde49133de84f2b1df5d23c588cf10`;
- schema contract: `2`;
- schema adversarial status: `implemented by x30.11`.

## Repository activation

### `papercuts`

- committed instruction SHA:
  `df35be1b93007d0026ca0911addf01bf37085978`;
- exact gated-binary, read-only, no-override, and private/balanced rules are in
  committed `AGENTS.md`;
- private doctor at activation: healthy, zero findings, zero checked lines;
- first real pilot signal after activation:
  `pc_2f56b98f7d76`, severity `minor`, tag `tooling`, policy decision `clean`;
- review found that one clause of that cut was based on reading `due` instead
  of the actual `due_at` field. Append-only correction resolved it with a clean
  note and logged only the independently verified envelope-shape friction as
  `pc_f6de23b7b19c`;
- the activation-session doctor was healthy with three checked lines and zero
  findings; folded state was two cuts, one resolved and one open;
- reminder setup later exposed a ClickUp hierarchy-count readback mismatch and
  recorded it as clean cut `pc_05ed812155c2`;
- current private doctor is healthy with four checked lines and zero findings;
  folded state is three cuts, two open and one resolved;
- worktree: clean and synchronized with `origin/main` at activation readback.

### `acfs-workbench`

- committed instruction SHA:
  `0ebaee7` (`chore: activate papercuts pilot`), pushed to `origin/main`;
- commit scope: only `AGENTS.md`, with the canonical exact-binary,
  read-only, no-override, and private/balanced rules;
- one real workflow cut was appended after activation:
  `pc_92d7ccde04df`, severity `minor`, tag `workflow`, policy decision `clean`;
- private doctor: healthy, zero findings, one checked line;
- private directory/file modes: `700/600`;
- Git status entries before/after private append: `27/27`;
- sanitized Git-status SHA-256 before/after:
  `70b4486aa64978fb4d786bfdd7f03decda499283af46e7f9fda19f105d6b56c9`;
- the before/after status strings were byte-identical. Existing unrelated
  worktree changes were not staged, edited, summarized into evidence, or
  treated as pilot output.

The first activation check correctly refused to infer pilot readiness from an
arbitrary `AGENTS.md` commit. Readback showed that the commit contained separate
CM guidance and no Papercuts marker. The pilot block was therefore added in a
new one-file commit and verified from `HEAD` before starting the clock.

## Initial synthetic policy checkpoint

The exact gated binary ran three `add --dry-run` scenarios against the
`papercuts` alias. Only category/field decisions were retained:

| Scenario | Exit | Decision |
|---|---:|---|
| balanced medium-risk fixture | 0 | `warn`, `email_address`, field `text` |
| strict medium-risk fixture | 65 | `sensitive_input`, `email_address`, field `text` |
| balanced high-confidence fixture | 65 | `sensitive_input`, `secret_assignment`, field `text` |

The private journal SHA-256 was identical before and after all three dry runs:
`b0d2315a6eb50e6a4d160b45954b12dce1a8d6e546994163d0e50f893e470256`.
No raw matched value was retained in this evidence.

## Authorized synthetic override checkpoint

After exact human authorization for one `email_address` dry-run fixture, the
gated binary ran the two-key override under strict policy. The matched fixture
value is deliberately omitted from retained evidence.

- exit: `0`;
- contract: `2`;
- policy mode: `strict`;
- decision: `override`;
- category: `email_address`;
- field: `text`;
- changed: `false`;
- write policy: `normal`;
- journal SHA-256 before and after:
  `b922f0dafcb5c8a167e4f6e827c900b64f6b56b705038cad7856e07a0efd6a49`;
- Git status before and after: byte-identical.

The authorization is exhausted. It does not apply to another category,
fixture, command, or real append.

Legacy-unscanned readback, ACFS review/resolve, rollback proof, elapsed reviews,
and the final signal/noise decision remain open in `x30.15`.

## Review calendar

| Gate | Not before | Assigned reminder |
|---|---|---|
| day 1 | `2026-07-13T16:07:01+07:00` | [`86ey8vpj4`](https://app.clickup.com/t/86ey8vpj4), due 16:15 +07 |
| day 3 | `2026-07-15T16:07:01+07:00` | [`86ey8vppn`](https://app.clickup.com/t/86ey8vppn), due 16:15 +07 |
| day 7 | `2026-07-19T16:07:01+07:00` | [`86ey8vppv`](https://app.clickup.com/t/86ey8vppv), due 16:15 +07 |
| day 14 | `2026-07-26T16:07:01+07:00` | [`86ey8vpqa`](https://app.clickup.com/t/86ey8vpqa), due 16:15 +07 |

Each review must use the exact binary, run private doctor and bounded list
readback, retain only sanitized counts/categories, compare Git state, and stop
on the conditions in `docs/PILOT_PLAN.md`. Immediate repeated probes do not
substitute for elapsed reviews.

Each reminder tells the operator to open the Codex chat named
**PaperCuts Project**. ClickUp provides the notification and entry point;
repository evidence remains authoritative.

## Rollback boundary

Rollback removes each repository instruction through ordinary Git history and
stops invoking the exact isolated binary. It does not delete, rewrite, merge,
or publish either private journal. The isolated installation root is not
removed during the pilot.
