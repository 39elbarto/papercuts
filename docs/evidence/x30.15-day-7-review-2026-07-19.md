# x30.15 Day-7 Pilot Review

Bead: `br-hardened-papercuts-fork-x30.15`

Review window: `2026-07-19T21:21:33+07:00` to
`2026-07-19T21:27:37+07:00`

## Verdict

`PASS / CONTINUE`. The checkpoint ran after its elapsed-time gate. The pilot is
producing useful, repeated workflow signals without disclosure, repository
pollution, or doctor findings. Keep the Papercuts core unchanged and continue
the same two-alias pilot through day 14, not before
`2026-07-26T16:07:01+07:00`.

The scheduled snapshot used the exact gated binary, profile `private`, and
`PAPERCUTS_READ_ONLY=1`. Resolution events were appended only afterward during
the authorized closeout.

## Identity and safety

- source SHA: `804d2b17e65edd865f3dc6e0ec05939aa65cf1ee`;
- installed binary SHA-256:
  `a74b2f926b72b7e4bc76039455e0e5cc8abde49133de84f2b1df5d23c588cf10`;
- machine contract: `2`;
- both doctors: healthy, zero findings;
- private-state directory/file modes: `700/600`;
- tracked journal files: zero in both repositories;
- disclosures, autonomous overrides, and private-to-committed fallback: zero.

## Read-only snapshot

| Alias | Journal lines | Cuts | Open | Resolved | Doctor findings |
|---|---:|---:|---:|---:|---:|
| `papercuts` | 8 | 6 | 4 | 2 | 0 |
| `acfs-workbench` | 17 | 16 | 15 | 1 | 0 |
| combined | 25 | 22 | 19 | 3 | 0 |

Fifteen cuts appeared since the day-3 checkpoint, all in `acfs-workbench`.
They span at least six observed work dates. Two were `major` and thirteen were
`minor`. Eight of the fifteen new records had no stable tag. Raw cut text,
resolution notes, timestamps, locators, and journal lines are not retained in
this public evidence.

## Signal classification

The fifteen new records contain thirteen distinct signals and two clear
same-alias duplicates. The repeated patterns are:

- ClickUp connector comment creation is advertised but unavailable at runtime;
- guarded temporary-file cleanup repeatedly needs a safe, explicit path;
- narrow Rust tests still pay a large full-dependency build cost;
- shell variable names can unexpectedly clobber zsh special arrays;
- smaller contract and setup mismatches appeared in CM, Beads, ShellCheck,
  hook removal, and an optional remote compilation dependency.

The ClickUp connector mismatch has now recurred across both aliases and reached
`major` severity. It is ready for promotion to the owning external tracker.
The cleanup and Rust build-cost clusters are also worth preserving for the
day-14 decision. No promotion Bead was created here: the correct external
owner is not established, and the ACFS Beads ledger already contains unrelated
worktree changes that this checkpoint must not touch.

Tagging quality needs a small integration improvement before wider use: eight
of fifteen new cuts were untagged. A stable tag should be encouraged or
required at capture time so periodic grouping does not depend only on semantic
review.

## Legacy-unscanned readback

An ignored, synthetic one-record fixture exercised the remaining legacy path.
Private `list` and `doctor` both reported
`legacy_path_records_retained:1` and `legacy_unscanned_records:1`; output used
the omitted path projection, doctor remained healthy, and the fixture SHA-256
was identical before and after. No raw path or synthetic text is included in
retained evidence.

This completes the legacy-unscanned scenario required by `x30.15`.

## Resolution closeout

Four resolution events were appended with the exact gated binary:

- `pc_69790f1a2586`: fixed documentation EOF issue, no recurrence;
- `pc_88b8c1e19e3d`: superseded by the canonical recurring ClickUp signal;
- `pc_bf0db5cc9a10`: duplicate of `pc_ff9624c2a7d3`;
- `pc_69717777b225`: duplicate of `pc_9f400c39b58e`.

Superseding a record does not claim the underlying external issue is fixed.
Post-resolution state:

| Alias | Journal lines | Cuts | Open | Resolved |
|---|---:|---:|---:|---:|
| `papercuts` | 10 | 6 | 2 | 4 |
| `acfs-workbench` | 19 | 16 | 13 | 3 |
| combined | 29 | 22 | 15 | 7 |

Both doctors remained healthy with zero findings.

## Git boundary

`papercuts` was clean and synchronized with `origin/main` before evidence
changes. `acfs-workbench` had 34 unrelated worktree entries with sanitized
status fingerprint
`520f7e8705e7d55d1a2945bfffc0e74f5cf1ff3e2478c4aff9c4c1cdbf37caec`.
Its committed `AGENTS.md` is clean, the journal remains outside Git, and HEAD
matches `origin/main`. The pilot did not alter or classify those unrelated
files.

## Metrics through day 7

- observed work sessions: at least 6;
- cuts: 22 total; 15 open and 7 resolved after closeout;
- new cuts since day 3: 15;
- distinct new signals: 13; same-alias semantic duplicates: 2;
- new severity: 2 major and 13 minor;
- new untagged records: 8 of 15;
- promoted issues: 0; external promotion candidates: 3 clusters;
- scanner false positives and suspected sensitive-data misses: 0 known;
- doctor findings, lock timeouts, permission failures, and disclosures: 0;
- unexpected pilot-caused Git mutations: 0;
- review, classification, legacy exercise, and closeout time: about 6 minutes.

## Recommendation

Do not change the Rust core at day 7. The strongest improvements belong to the
operating layer: better tag consistency, duplicate-aware review, and promotion
of recurring external-tooling clusters to their real owners. Keep collecting
until day 14 before deciding broader adoption.

## ClickUp closeout

After commit `3fba758` was published, the approved local bridge preserved the
day-7 reminder, appended the sanitized result, and changed task `86ey8vppv` to
`complete`. Official readback confirmed the result text, status, and parent
`86ey8k1ay`. The parent work date was refreshed to July 19, day-14 task
`86ey8vpqa` remains `inbox`, and the bridge host stopped.

## Remaining x30.15 evidence

- elapsed day-14 review;
- rollback proof for both aliases;
- final signal/noise, false-positive, operator-cost, and safety totals;
- handoff to `x30.16` for the wider-adoption decision.
