# x30.15 Papercuts Pilot Activation

Date: 2026-07-12

Repository alias: `papercuts`

Status: activated locally; the combined 14-day clock remains stopped until
`acfs-workbench` also passes activation.

- Operator approved removal of the tracked legacy `.papercuts.jsonl` from the
  current repository version.
- The 20 development dogfood records were not copied into pilot storage. They
  remain recoverable in ordinary Git history; no history rewrite occurred.
- Created a fresh empty private journal under the Git common directory.
- Private directory mode: `700`; journal mode: `600`.
- Exact gated binary SHA:
  `804d2b17e65edd865f3dc6e0ec05939aa65cf1ee`.
- Binary SHA-256:
  `a74b2f926b72b7e4bc76039455e0e5cc8abde49133de84f2b1df5d23c588cf10`.
- `doctor`: healthy, zero findings, zero checked lines.
- Private list: zero records; profile `private`, source `profile-default`, path
  policy `omitted`.
- Added the exact-path, read-only, no-override, and non-sensitive pilot rules to
  this repository's `AGENTS.md`.

Rollback: revert the focused tracked-file/AGENTS commit and stop invoking the
pilot binary. Do not rewrite the new private journal. The historical committed
journal can be inspected from an earlier Git revision if needed.
