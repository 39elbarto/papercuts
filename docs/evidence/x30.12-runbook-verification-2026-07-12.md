# x30.12 Runbook Verification Evidence

Date: 2026-07-12

Base commit: `b8967a5f0a3821eddd7799abae5768378eb4cb68`

Scope: contract-2 single-project instructions and operator workflow

## Documentation contract

- `docs/AGENTS_PAPERCUTS_SNIPPET.md` is the canonical copy-ready agent block.
- `docs/SINGLE_PROJECT_RUNBOOK.md` owns install, preflight, storage selection,
  read-only behavior, review/resolve, migration, limitations, verification, and
  selection-only rollback.
- README now describes private as the default and committed as an explicit
  exposure lane. It no longer points hardened-fork users at the upstream
  registry package or describes repository-visible storage as the default.

## Executed lifecycle

Command:

```bash
scripts/verify-single-project-runbook.sh
```

Result: `single-project runbook verification: pass`.

The script built and locally installed the current tree into an isolated
temporary root, then exercised schema contract 2, private add/list/resolve and
doctor, unchanged Git status, user-only permissions, read-only refusal without
byte changes, linked-worktree sharing, explicit non-Git private storage,
committed exposure warnings, migration refusal/copy/readback, and committed
rollback selection. It emits only a sanitized result line and removes its
fixtures on exit.

The documented remote exact-SHA `cargo install --git ... --rev` command was
also exercised against base commit `b8967a5`. The online attempt reached the
correct Git commit but crates.io was temporarily unavailable; `--offline`
completed from cached dependencies. Its preflight then exposed a real status
drift: schema still said `pending x30.11`. The current change corrects that to
`implemented by x30.11`, pins it in the CLI test, and makes the runbook assert
the real `path_projection` key and `implemented by ...` value form.

## Quality gate

```text
cargo build --release                                                       pass
cargo test --all-features                                                   pass: 17 unit + 53 CLI + 10 acceptance
cargo clippy --all-targets --all-features -- -D warnings                    pass
cargo fmt --check                                                           pass
bash -n scripts/verify-single-project-runbook.sh                             pass
scripts/verify-single-project-runbook.sh                                    pass
git diff --check                                                            pass
gitleaks detect --source . --no-git --redact --exit-code 1                  pass: no leaks
papercuts --profile private --file .papercuts.jsonl doctor                  pass: healthy, 20 lines
```

Scoped UBS was run on the schema and CLI contract test. Its non-zero report is
the existing test-only panic/assert inventory, false-positive secret
comparisons on warning labels, bounded JSON indexing, and fixture allocations.
It independently reported clean formatting, Clippy, Cargo check/test build,
unsafe usage, runtime regex, shell execution, hardcoded-secret detection, and
resource lifecycle. The only production change is a static implementation
status string.

## Rollback

Revert the focused documentation/status commit. Do not rewrite either private
or committed journals. A binary rollback is selection-only and must record the
loss of contract-2 protections when selecting upstream v0.1.
